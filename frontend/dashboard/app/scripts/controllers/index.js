'use strict';

angular.module("tukdesk")
    .controller("indexCtrl", ["$scope", "$location", "globals", "api", function($scope, $location, globals, api) {
        console.log("index ctrl");
    }]);
